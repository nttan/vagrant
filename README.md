1. package a box
command:    
  vagrant package ubuntu14 --vagrantfile Vagrantfile


2. Import new box 
command:
  vagrant box add my-box /path/to/the/new.box
  
  vagrant init my-box
  
  vagrant up
